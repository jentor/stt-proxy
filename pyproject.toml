[project]
name = "stt-proxy"
version = "0.1.0"
description = "OpenAI Audio API compatible proxy for Yandex SpeechKit and Salute Speech"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "stt-proxy maintainers" },
]
license = { text = "MIT" }
keywords = ["stt", "speech-to-text", "openai", "yandex", "salute-speech", "proxy"]

dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-multipart>=0.0.20",
    "pydantic-settings>=2.7.0",
    "httpx>=0.28.0",
    "salute-speech>=2.0.0",
    "yandex-ai-studio-sdk>=0.20.0",
]

[project.scripts]
stt-proxy = "app.main:run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
    "ruff>=0.15.17",
]
